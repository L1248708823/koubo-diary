# 捕捉端采用 B 在场感手感

对比 A 极简投递、B 在场感、C 角上发送三套可点原型后，选定 B 作为 v1 捕捉端。

B 保留底部全宽「送进收件箱」、空态禁用不跳布局、成功清空回焦、失败保字；并纳入「今日已投」计数、可折叠加列表与短时撤销。分类仍由处理端决定。不采用 C 的角上发送（拇指目标与文案醒目度不足），不做按住说话或自动开麦。

详见 `temp/capture/NOTES.md` 与 `temp/capture/b.html`。
