---
name: 处理收件箱
description: 处理收件箱：将本轮口播轻整理写回当日日记，按双轴规则可创建顶层想法，并写出机器可读回执。
---

# 处理收件箱

执行固定的处理闭环：清点 → 判断 → 写日记 → 写想法 → 写回执 → 审计。

编排脚本已经拿到单实例锁、准备工作区，并在提示中给出本轮 `pendingInbox`。你运行在 vault 根目录，只处理这份清单，不扫描 `_inbox/_quarantine/`，也不扩大单轮范围。

## 路径契约

以运行提示中的配置为准；默认目录如下：

- 日记：`生活/日子一天天过去/YYYY/YYYY-MM/YYYY-MM-DD.md`
- 想法：`想法/短标题.md`
- 收件箱：`_inbox/*.md`
- 状态与回执：`_processor/STATE.md`、`_processor/last-run.json`

想法路径必须直接位于配置的 `IDEAS_DIR` 下，形式只能是 `<IDEAS_DIR>/文件名.md`。`生活/**/想法/**`、按年或按月分桶、`想法/子目录/文件.md` 都不是合法想法路径。配置目录名变化时，只替换目录名，不改变这个层级规则。

允许写入的范围只有日记树、顶层想法目录、`_staging/` 和 `_processor/`。收件箱只读。Git 与收件箱生命周期由编排脚本接管：本轮保持工作树状态，不执行 Git 命令，不删除、移动或重命名 `_inbox/` 文件。

## 双轴判断

每条口播独立判断两条轴，结果可以组合：

| 轴 | 判断 | 写回 |
| --- | --- | --- |
| 想法 | 去掉日期、时间和当下情绪后，仍值得离开今天单独回看吗？ | 是则创建一条顶层想法，否则只写日记 |
| 待查 | 是否存在事实、资料、对比或可行性方面的未决缺口？ | 是则标记 `needs_research: true`，只写清要弄清什么 |

想法宁少勿滥。原则、假设、产品或生活点子、可复用方法更适合独立成想法；纯流水、情绪或无法脱离当天语境的内容只进入日记。吃不准时不创建想法，也不标待查。待查不触发外网搜索，不写未经验证的结论。

## 处理闭环

### 1. 清点

读取提示中的每个 inbox 文件，保留原意和说话口气，去掉明显口头赘词与重复，按需要断句分段。完成条件：本轮清单中的每个文件都已进入后续判断，且没有处理清单外的文件。

### 2. 判断

为每条口播确定 `done`、`failed` 或 `quarantine`，并确定想法轴与待查轴结果。无法安全处理时使用 `failed`；只有明显毒数据才使用 `quarantine`。完成条件：每条清单项都有唯一状态，且想法与待查判断能从原文得到依据。

### 3. 写回日记

每条 `done` 都更新对应日期的日记文件。没有文件时按路径契约新建，有文件时追加并保留已有段落。无想法时写时间戳和轻整理短段；有想法时写时间戳、一句钩子和 `[[<IDEAS_DIR>/短标题]]`，不在日记重复想法全文。完成条件：每条 `done` 都有真实存在的 `diary` 文件。

### 4. 写回想法

只为想法轴判定为是的条目创建文件，一条想法一个文件。标题冲突时使用 `短标题-YYYYMMDD.md` 或 `短标题-2.md` 等新文件，不把新内容静默追加到旧文件。

想法文件必须直接位于 `<IDEAS_DIR>/`，正文使用轻整理全文，并包含 `created`、`source_diary`、`needs_research` frontmatter；待查时在正文顶部用一两句写清要弄清什么。可在文末保留链回当日日记。完成条件：每个 `idea` 路径都符合顶层规则、文件存在，并与日记互链；没有 `idea` 时不写假路径。

### 5. 写回执

覆盖写入 `_processor/last-run.json`，使用以下结构：

```json
{
  "ok": true,
  "round_ended_at": "ISO8601",
  "processed": [
    {
      "inbox": "_inbox/YYYYMMDD-HHMMSS-id.md",
      "status": "done",
      "diary": "生活/日子一天天过去/2026/2026-07/2026-07-30.md",
      "idea": "想法/短标题.md",
      "needs_research": false,
      "notes": ""
    }
  ],
  "failed": [],
  "quarantine": []
}
```

`status` 只能是 `done`、`failed`、`quarantine`。每条本轮 inbox 必须且只能出现在一个数组中；`done` 必须有 `diary`，声明 `idea` 时必须使用合法顶层路径；无想法时省略 `idea`。回执只描述已经写入的事实，不用自然语言替代路径或状态。

### 6. 审计

重新检查回执能被解析，检查每条 `done` 的日记和想法文件确实存在，检查本轮 inbox 均已交代，检查没有删除或移动 inbox。完成条件全部满足后停止，不进行无边界的二次改写，也不执行 Git 操作。

整理后的文字仍应像使用者本人随口说的话，不写成公众号、日报或助手总结，不擅自扩写、升格文风、代下结论或开放式添加主题标签。
