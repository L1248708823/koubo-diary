# 04 — 捕捉端接真投递

**What to build:** 捕捉端（既有 B 在场感手感）配置收件接口地址与 Bearer 后，能把口播**真投递**进服务端收件箱；成功则清空输入并回焦、可继续连倒；失败则正文保留并可见原因。捕捉端仍只新建收件箱语义所要求的投递，不写日记/想法正文，不选手动分类。不要求本票内重做视觉，只接真契约；处理轮可仍由 03 的唤醒或后续托底消化。

**Blocked by:** 02 — 收件接口：鉴权投递只进收件箱

**Status:** done

- [x] 可配置 Ingest URL 与 token（不进 git 秘密；本地或设备侧保存）
- [x] 成功投递：服务端收件箱出现新条目；UI 清空并回焦
- [x] 失败投递（网络/鉴权/校验）：输入区文字保留，有可理解提示
- [x] 连续多条投递无需切换页面
- [x] 捕捉端无「写日记/想法正文」入口，无强制手动分类
- [x] 对照 B 手感：今日已投在场感与既有成功/失败规则不被破坏（允许接 API 后计数改为可信来源或保持可说明的本地策略）

## Comments

- 2026-07-29 implement：`capture/index.html`（B 手感 + 设置面板 localStorage）。短时「撤销」改为正文回填说明，不删服务端 inbox。端到端真服务验收等 VPS。
