# 口播日记

个人最小闭环：把语音输入法转写出的口播，经 AI 整理后写入已有的 Obsidian vault（日记合并或想法落盘），不自研语音识别，不做多用户产品。

## Language

**口播**:
一段由语音输入法转写得到、等待进入流水线的原始文本。
_Avoid_: 录音, 语音笔记, 口述稿

**收件箱**:
vault 内只追加、不改正文的投递区；捕捉端只能在此新建文件。
_Avoid_: temp, 草稿箱, 缓存

**捕捉端**:
负责在灵感出现时抓住口播并投递到收件箱的入口，不写日记或想法正文。
_Avoid_: App, 前端, 客户端（除非特指实现形态）

**处理端**:
读取收件箱、调用 AI 整理、写入日记或想法、并清理收件箱的权威一侧。
_Avoid_: 后端, 服务端, worker（除非特指实现形态）

**日记**:
按日存在的回顾正文；同一天多条口播应合并进同一篇，不另起多篇「今日日记」。
_Avoid_: daily note（可在实现层使用）, 日志, 日志文件

**想法**:
独立于日记保存的灵感条目，落在想法区并与当日日记互链。
_Avoid_: 备忘, 笔记, 卡片

**整理**:
AI 对口播做的有限加工（如去重复、保语气），不是重写作者立场。
_Avoid_: 润色, 改写, 生成

**投递**:
捕捉端把一条口播成功写入收件箱的动作；成功后输入区清空并回焦，失败则正文保留。
_Avoid_: 提交, 上传, 保存（口语里可说「送进去」，领域词用投递）

**处理环**:
处理端持续承担的职责：观察收件箱、整理并写入日记或想法、在成功验收后清理已处理项；单轮可无事退出，职责本身不消失。
_Avoid_: 定时任务（可指实现）, 批处理作业, while 死循环, Goal（Goal 是有终点的一次目标）

**回执**:
一轮处理结束时由 AI 写出的机器可读结果声明，列出各收件箱条目的处理意向与写回路径，供脚本核对。
_Avoid_: 日志（泛指）, 聊天总结, 报告

**隔离区**:
多次自动处理仍失败后，收件箱条目被移入、不再被处理环自动啃食的区域。
_Avoid_: 垃圾箱, 失败文件夹（口语可说失败堆，领域词用隔离区）

**待查**:
口播被标记为需要后续了解或调研、但本轮不执行调研的状态。
_Avoid_: 待办, 调研中, needs research（实现层字段可保留英文）
