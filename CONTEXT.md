# Yan帳

个人最小闭环：把捕捉端产生的原始记录，经 AI 整理后写入已有的 Obsidian vault（日记合并或想法落盘），产品主题为 Yan帳。不自研语音识别，不做多用户产品。

## Language

**Yan帳**:
产品主题和用户可见总称，代表从记录、整理到长期内容回看的个人系统。vault 顶层的 `Yan帳/想法` 与 `Yan帳/研究` 是其中的长期内容空间。Yan帳 不是单条原始记录，也不是 AI 收集区。
_Avoid_: 旧产品称呼, AI 收集区, AI 资料夹

**收件项**:
一条已经投递、等待处理的原始记录；正文和捕捉时间属于使用者的原始内容，处理端只能在验收后由脚本清理，验收完成后不另外保存收件项原文。
_Avoid_: 草稿, 临时笔记, 任务

**收件箱**:
vault 内只追加、不改正文的投递区；捕捉端只能在此新建收件项。
_Avoid_: temp, 草稿箱, 缓存

**捕捉端**:
负责在灵感出现时抓住原始记录并投递到收件箱的入口，不写日记或想法正文。
_Avoid_: App, 前端, 客户端（除非特指实现形态）

**处理端**:
读取收件箱、调用 AI 整理、写入日记或想法、并清理收件箱的权威一侧。
_Avoid_: 后端, 服务端, worker（除非特指实现形态）

**日记**:
按日存在的回顾正文；同一天多条收件项应合并进同一篇，每条收件项单独成段并按捕捉时间排序，不另起多篇按记录分割的日记。整理只在单条收件项内部进行，不跨收件项合并内容或删除重复。保留日期、过程、现场语境、个人声音和未决问题，落在使用者既有的按日树中（路径见 ADR-0006）。
_Avoid_: daily note（可在实现层使用）, 日志, 日志文件

**想法**:
可脱离「今天」单独回看的灵感、原则、假设或方法；只有内容明确表达使用者的观点、假设、创意或方法，脱离日记仍能理解且值得回看，并且不需要处理端补背景、理由或结论时，才自动创建想法文件。固定句式只作提示，只有问题时标待查；模糊念头先留在日记里。一条收件项里有多个互不相关且各自能看懂的想法时分别创建文件，拆开后需要补上下文时不拆。一条一文件落在 `Yan帳/想法/`，并与源日记互链。与是否进入研究任务无关，不是日记的按日分桶。
_Avoid_: 备忘, 笔记, 卡片, 灵感杂烩（指把多条独立念头塞进同一时间桶文件）

**研究任务**:
在日记或想法上存在认知缺口，并被确认进入研究流程后，主动搜集资料、交叉验证和整理多种观点的一次任务。研究任务可以来源于日记，也可以来源于想法。
_Avoid_: 疑问, 待办, 自动搜索

**研究简报**:
研究任务的独立输出，包含问题、结论、证据、来源链接、不同观点、未知点和研究时间；落在 `Yan帳/研究/`，并与来源日记或想法互链。
_Avoid_: 搜索结果堆, 聊天总结, 无来源结论

**整理**:
AI 对收件项做的有限加工：只改明显错别字、标点、断句和排版；只有确定删除后不影响意思、情绪和语气时，才删除重复或停顿词，拿不准就保留。同一个词连续出现后马上改口或纠正的重复可以删除，为了表达情绪或强调而重复的内容必须保留。整理必须保留原有语气、情绪、犹豫和未决问题，不得摘要或重写作者立场。
_Avoid_: 润色, 改写, 生成

**投递**:
捕捉端把一条原始记录成功写入收件箱的动作；成功后输入区清空并回焦，失败则正文保留。
_Avoid_: 提交, 上传, 保存（口语里可说「送进去」，领域词用投递）

**处理环**:
处理端持续承担的职责：观察收件箱、整理并写入日记或想法、在成功验收后清理已处理项；单轮可无事退出，职责本身不消失。
_Avoid_: 定时任务（可指实现）, 批处理作业, while 死循环, Goal（Goal 是有终点的一次目标）

**回执**:
一轮处理结束时由 AI 写出的机器可读结果声明，列出各收件箱条目的处理意向与写回路径，供脚本核对。
_Avoid_: 日志（泛指）, 聊天总结, 报告

**隔离区**:
多次自动处理仍失败后，收件箱条目被移入、不再被处理环自动啃食的区域。
_Avoid_: 垃圾箱, 失败文件夹（口语可说失败堆，领域词用隔离区）

**待查**:
日记或想法上存在未决认知缺口的候选标记；与是否建想法文件相互独立。待查不等于研究任务，只有满足研究触发条件并进入研究流程时才建立研究任务。
_Avoid_: 待办, 调研中, 疑问（口语可说疑问，领域词用待查）, needs research（实现层字段可保留英文）
